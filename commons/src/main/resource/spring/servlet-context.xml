<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd">
	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	<!-- Scans within the base package of the application for @Components to configure as beans -->
	<!-- @Controller, @Service, @Configuration, etc. -->
	<context:component-scan base-package="com.company.web" />

	<context:property-placeholder location="classpath:spring/velocity-config.properties"/>
	<!-- Enables the Spring MVC @Controller programming model -->
	<mvc:annotation-driven />
	<mvc:default-servlet-handler/>

	<!-- integration Velocity -->
	
	<!-- velocity config -->
	<bean id="velocityConfig"
		  class="org.springframework.web.servlet.view.velocity.VelocityConfigurer">
		  <property name="resourceLoaderPath" value="${view.root.path}" />
		  <property name="configLocation" value="${velocity.config.file}" />
	</bean>
	
	<!-- velocity view resover-->
	<bean id="velocityViewResolver"
		  class="org.springframework.web.servlet.view.velocity.VelocityViewResolver">
		<property name="cache" value="${cacheable}" />
		<property name="suffix" value="${view.suffix}" />
		<property name="contentType" value="${content.type}" />
		<property name="exposeRequestAttributes" value="${exposeRequestAttributes}"/>
		<property name="exposeSessionAttributes" value="${exposeSessionAttributes}"/>
		<property name="toolboxConfigLocation" value="${toolboxConfigLocation}"/>
		<!-- custom properties ;used in view-->
		<property name="attributes">
			<props>
				<prop key="ver">0.0.1</prop>
			</props>
		</property>
	</bean>
	
	<bean class="org.springframework.web.servlet.view.ContentNegotiatingViewResolver">
		<property name="defaultContentType" value="text/html;charset=utf-8" />
		<property name="ignoreAcceptHeader" value="true" />
		<!-- xml and json configuration -->
		<property name="mediaTypes">
			<map>
				<entry key="xml" value="application/xml"></entry>
				<entry key="json" value="application/json"></entry>
			</map>
		</property>
		
		<property name="viewResolvers">
			<list>
				<ref bean="velocityViewResolver"/>
			</list>
		</property>
		
		<property name="defaultViews">
			<!-- for application/json -->
			<bean class="org.springframework.web.servlet.view.json.MappingJacksonJsonView"></bean>
		</property>
	</bean>
</beans>

